# Secrets
config.json

# Claude Code local settings (may contain secrets)
.claude/

# Sessions
*.session
*.session-journal

# Tracker state
danang-news-posted.json

# Runtime state
*_state.json

# HTTP cache
cache/

# Virtual environment
venv/

# Logs
logs/
*.log

# Python
__pycache__/
*.pyc

# OS
.DS_Store

# Runtime-состояние, появившееся при наведении порядка
heartbeats.json
price_cache.json
*.lock
*state.json.corrupt-*

# Атомарная запись (.tmp + os.replace) и карантин повреждённых файлов
# (не только *_state.json — тем же способом квалифицируется и трекер)
*.tmp
*.corrupt
*.corrupt-*

# Скриншоты и прочие временные материалы, не место в портфолио-репозитории
Временные файлы/
